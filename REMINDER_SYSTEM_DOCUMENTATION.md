# Employee Reminder Popup System Documentation 🔔

यह डॉक्यूमेंटेशन समझाता है कि **Gharplot CRM** में एम्प्लॉई रिमाइंडर पॉप-अप सिस्टम कैसे काम करता है और हाल ही में इसमें क्या सुधार किए गए हैं।

---

## 🏗️ सिस्टम आर्किटेक्चर (How it Works)

रिमाइंडर सिस्टम तीन मुख्य हिस्सों पर आधारित है:

1.  **Database (MongoDB):** रिमाइंडर्स की जानकारी, समय और एम्प्लॉई के `fcmToken` को स्टोर करता है।
2.  **Cron Job (`reminderCron.js`):** हर 1 मिनट में डेटाबेस चेक करता है कि किसका रिमाइंडर ड्यू (Due) हो गया है।
3.  **FCM Service (`fcmNotificationService.js`):** Firebase के जरिए एम्प्लॉई के डिवाइस पर असली "पॉप-अप" भेजता है।

---

## 🛠️ हाल ही में किए गए सुधार (Recent Fixes)

### 1. Circular Dependency Fix (सर्वर क्रैश इशू)
**समस्या:** `server.js` और `reminderCron.js` एक-दूसरे को इम्पोर्ट करने की कोशिश कर रहे थे, जिससे सिस्टम अटक जाता था और Cron Job शुरू ही नहीं हो पाता था।
**समाधान:** हमने `reminderCron.js` में `initReminderCron(io)` फंक्शन बनाया है। अब यह सर्वर के पूरी तरह तैयार होने के बाद ही स्टार्ट होता है।

### 2. Payload Enhancement (बेहतर नोटिफिकेशन)
**सुधार:** पहले नोटिफिकेशन में सिर्फ क्लाइंट का नाम जा रहा था। अब इसमें **Reminder Title** और **Reminder ID** भी शामिल हैं, जिससे ऐप को पता चलता है कि कौन सा रिमाइंडर खुला है।

### 3. iOS (APNs) Support
**सुधार:** iOS डिवाइसेस के लिए नोटिफिकेशन बॉडी में टाइटल मिसिंग था, जिसे फिक्स कर दिया गया है।

---

## 📋 एम्प्लॉई के लिए आवश्यकताएँ (Requirements)

रिमाइंडर पॉप-अप प्राप्त करने के लिए एम्प्लॉई के पास निम्नलिखित होना अनिवार्य है:
1.  **FCM Token:** एम्प्लॉई के लॉगिन करने पर ऐप से टोकन डेटाबेस में `fcmToken` फ़ील्ड में सेव होना चाहिए।
2.  **Active Status:** एम्प्लॉई का अकाउंट `isActive: true` होना चाहिए।
3.  **Permissions:** मोबाइल ऐप में "Notification Permission" को Allow किया जाना चाहिए।

---

## 🧪 सिस्टम को टेस्ट कैसे करें (How to Test)

### Step 1: सर्वर लॉग्स चेक करें
सर्वर स्टार्ट करते समय देखें कि यह मैसेज आता है या नहीं:
`⏰ Reminder cron initialized - FCM notifications enabled`

### Step 2: हर मिनट की चेकिंग देखें
टर्मिनल में हर 60 सेकंड में यह मैसेज आना चाहिए:
`🔍 Checking reminders...`

### Step 3: मैन्युअल टेस्ट स्क्रिप्ट (Manual Test)
मैंने एक स्पेशल टेस्ट स्क्रिप्ट बनाई है जो सीधे एक नोटिफिकेशन भेजती है:
```bash
cd backend
node test-trigger-fcm.mjs
```
*यह स्क्रिप्ट डेटाबेस से किसी एक एम्प्लॉई को चुनकर तुरंत टेस्ट पॉप-अप भेजेगी।*

---

## 📁 मुख्य फ़ाइलें (Key Files)

- `backend/server.js`: सिस्टम को शुरू (Initialize) करता है।
- `backend/cron/reminderCron.js`: टाइमिंग और लॉजिक को हैंडल करता है।
- `backend/utils/fcmNotificationService.js`: नोटिफिकेशन भेजने का मुख्य इंजन।
- `backend/test-trigger-fcm.mjs`: टेस्टिंग टूल।

---

## ⚠️ सामान्य समस्याएँ (Troubleshooting)

- **रिमाइंडर नहीं आ रहा?** चेक करें कि डेटाबेस में उस एम्प्लॉई के पास `fcmToken` है या नहीं।
- **कंसोल में एरर?** अगर `invalid-registration-token` आता है, तो इसका मतलब है कि एम्प्लॉई ने ऐप अनइंस्टॉल कर दी है या नोटिफिकेशन डिसेबल कर दिए हैं।
- **समय का अंतर?** सर्वर का टाइमज़ोन (UTC/Asia/Kolkata) चेक करें। सिस्टम वर्तमान में भारतीय समय (Asia/Kolkata) के अनुसार ड्यू रिमाइंडर्स चेक करता है।
